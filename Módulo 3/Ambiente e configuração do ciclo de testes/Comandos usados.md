```
yarn init -y                    //Para inicializar o projeto com a criação do package.json

npm install axios               //Para consumir a api do star wars e conseguir usar os serviços, criando o package-lock.json e o node_modules

npm install -g mocha            //Para instalar o mocha globalmente

npm install --save-dev mocha    //Para instalar o mocha como dependência de desenvolvimento

npm install nock                //Para simular requisições

```